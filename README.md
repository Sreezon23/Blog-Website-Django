# Blog-Website-Django

Make sure you have all the necessary dependencies to run a Django project.

Django Jazzmin was used to design the Admin Dashboard of this project. So that is necessary as well.

After that, download this project as a zip, extract the files and then from Barca Buzz directory run "python manage.py runserver"
